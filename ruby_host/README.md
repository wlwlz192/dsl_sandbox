This is a simple example register DSL using Ruby as the host language. The output is a JSON formatted register spec of sorts.

register.rsl is the actual DSL content.

```
    ruby rsl.rb register.rsl
```

